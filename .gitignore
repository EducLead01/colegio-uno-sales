# Arquivos-fonte de mídia: pesados e já convertidos para os formatos web.
# Ficam só na máquina local, não no repositório.
*.MOV
*.mov
subs.jpeg

# Cópia local do site unosales.com.br (páginas + 46 MB de fotos).
# Material de origem pra garimpar imagens; não entra no repositório.
site-unosales/
